# P6：最终矩阵、报告和交付审查

## 文件归属与验收

- **Reuse `Invoke-SceneRaid.ps1`、`Invoke-SceneRaidPlayer.ps1`、`SceneRaid.Report.psm1`、`SceneRaid.Contracts.psm1`**：继续同一常驻 Editor 的正式场景和可见 Player 验证。目标由游戏选择，自动化只操作背包。
- **Create `outputs/scenezl_final1_validation.json`**：保存有限矩阵、逐轮玩法结局、终态/仓库契约、性能、完整输入和原始证据 SHA256，区分历史失败与当前有效证据。
- **Create `outputs/scenezl_final1_validation_report.md`**：解释工程完成状态、修复链、热点前后对比、最终 1× 帧率、真实覆盖和未解决限制。
- **Extend 本目录 `task_plan.md`、`p5_player_verification.md`、`architecture_review.md`**：回写大规划完成情况和最终审查。此阶段不再修改 Gameplay，也不把文档生成器移入运行时代码。
- **Extend 根目录 `README.md`**：补充本次实际验收场景、自动验证能力和最终报告入口，区分菜单入口与已验证场景，更新构造目录数量。这里只同步工程状态，不调整游戏入口。
- **Extend `tools/agent-repro/README.md`**：矩阵全部结束后同步构造目录数量、两个新组和报告入口，示例指向当前保留工作区；此文档属于采集输入，因此在冻结期间不修改。

矩阵采用同一修正后源码和场景：731 三轮 4×，1731/2731 各一轮 4×，731 的 1× Editor 和 1× Player。最终七轮要求全部输入的路径、字节数和 SHA256 一致，包括 Automation、Editor 测试、tools/agent-repro；另外保留 Gameplay/资产指纹和场景 SHA256 便于核对。

每轮必须证据完整、正常结束、零程序错误/失败指令/停滞，全部撤离或经严格核对的正常战死均有效；至少一轮实际完成全员搜打撤，全部已定义覆盖逐项在同一矩阵内核对。反击恢复可以来自随后正常战死的回合，不要求所有覆盖和全员生还恰好发生在同一轮。1× Editor 和 Player 平均 FPS 均严格 >60，尾帧只诊断。Player 必须真实 4K / High Fidelity 渲染、源码与二进制未变、正常退出。历史退出/刷新故障及稀有 NoProgress 不因为当前通过而声称已修复。

## 验证过程记录

以下保留各次停止矩阵和调整的历史，最终状态以文末交付结果为准。

P5c 后第三轮 731 出现一次 Search/Unreachable，虽然两人最终撤离，仍停止矩阵并保留问题轮 `012842-945`。调查构造确认独立的高 baseOffset 资源查询缺陷，P3j 修复及相关回归完成，提交 `c0e4cb9`。

**最终矩阵改为 c0e4cb9 后全部重新运行，要求完整输入指纹一致。** 先前 P5c 自动化版本之间允许复用的方案不再用于最终验收；历史数据继续保留，只有新矩阵完成后生成交付文件。

该矩阵前四轮通过，第五轮 `015149-132` 出现两次箱边 NoProgress，停止最终验收。P3k 取得双角色阻挡的 1×/4× 红灯后，修正场景 Agent 1 半径覆盖并完成回归，提交 `0c79cc8`，随后重新冻结输入执行矩阵。

P3k 后五轮 4×通过，但 `020752-208` 的 1× Editor 在另一箱子出现正常半径角色的同箱占位冲突，仍停止矩阵；此轮 102.19 FPS 和正常死亡结算不能抵消行为失败。P3l 的实际占位修复、构造和审查完成，提交 `804e78d`，最终矩阵再次全部重新执行，先跑 1×731，再跑五轮 4×和 Player。

按用户已确认的正常战死约定，最终汇总不额外要求“反击恢复与全员生还必须同轮”；保存每个覆盖项对应的原始 runId，至少保留一轮全员搜打撤。没有减少轮数、丢弃行为错误或借用旧版本覆盖。

## 最终交付结果

`804e78d` 后的新矩阵已完成：七轮全部通过，5 轮双人撤离，2 轮正常战死结局。五轮 4×验证逻辑，正常速度 Editor 平均 95.23 FPS，Player 平均 236.22 FPS，每轮独立通过证据、行为、终态和结算核对。本次先复核正常速度 731，再运行其余固定种子，未再出现箱边 NoProgress。

完整输入指纹：`e0b1674ca71f7906d5ba96e374bf6939da50c2761c41df8265908d90bf96fb4a`。七轮均包含同一 Automation、Editor 测试、工具、场景和资产输入，没有混用修复前绿色数据。Player 真实渲染、进程正常退出、二进制和源码未变，Editor 保留。

已交付 [验收报告](../../outputs/scenezl_final1_validation_report.md)、[机器证据](../../outputs/scenezl_final1_validation.json)，同步 README、大规划、P5 和最终架构审查。P3l 的三项占位边界、六项旧场景对照、两项持续到达及 37 项相邻回归通过，既有各阶段构造和故障探针保留独立成绩；没有将重叠次数累加为一次全项目测试。

剩余限制：历史 Editor 原生挂起、一次瞬时 Unreachable 的根因尚未独立确认，场景缺脚本/资产告警保留；有限矩阵通过不宣称无限运行或所有场景无缺陷。用户 FFT inactive 工作区修改没有纳入代码修复提交。
