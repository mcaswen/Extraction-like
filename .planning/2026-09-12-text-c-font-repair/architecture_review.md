# text-c 字体修复架构审查

2026-09-12，结论：通过。

- 修复归属于字体源资产及其 TMP 派生缓存。未在字符串格式化、Presenter、指令事件或场景中加入字符替换，未改 UI 公共接口和模块依赖。
- 离线轮廓修复位于 `tools/font-repair/repair_text_c.py`；只从同字体取部件，不引入另一个字体的风格。脚本要求已知错误前提，候选写到独立路径，经差异校验才回传资产。
- 仅 `uni6307` 轮廓和左侧 bearing 改变，advance 保持 1000。全字形编译结果、cmap 和表级差异校验阻止对其他字形及无关字体表的静默重写。7,782 个 glyph 中 7,781 个不变。
- TMP 维护责任独立在 Editor/FontMaintenance，未挤入 Bangers 专用维护工具或 Gameplay。整表重建失败后，采用已规划的局部更新；保留其他 3,630 个字形的度量和 UV、全部 3,631 个字符记录、材质/图集 fileID 与 GUID。
- 新图集像素仅写入空白格位，旧格位保持原打包占用，避免破坏其他 free-rect 记录。维护入口对本次已修复的度量直接返回，不在重复调用中持续申请空间。纹理可读性恢复为原值 false，图集尺寸和数量不变。
- 图形测试复用原正式 HUD、Dispatcher、截图和隔离运行器，在同一帧比较指/址实际像素；TMP 另用真实缓存渲染。断言失败清理也由 finally 保证相机解绑。未增加运行时代码或逐帧分配。
- 红灯 `20260912-174711-280` 显示 0 像素差异；最终 `20260912-175651-635` 中 Text/TMP 分别 374/448 像素差异，成功/失败/淡出通过。维护工具编译错误、满图集重建失败均保留日志，没有当作通过证据。最终截图逐一读取，原本遗漏的字形错误已纠正。
- 本次不新增模块依赖、场景变更或架构边界，符合已授权的小规划。验证范围限当前字形及相邻显示链路，不声称整套字体全部汉字均正确。
