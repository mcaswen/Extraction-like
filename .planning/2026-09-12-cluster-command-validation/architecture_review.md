# Cluster 指令验证：规划架构自审

日期：2026-09-12。对象：[task_plan.md](task_plan.md)。状态：**规划已获确认，P0–P3e 实施审查完成，P3f 资源完成时序修复和 P4 最终验收进行中。**

## 1. 需求与事实

- 用户明确只看 Cluster，已排除 Zone。正式入口是 PlayerInputManager → Dispatcher；旧 DB 点击脚本和 Room/Zone 原型没有被当成当前生产链。
- 实际场景通过 TargetInputManager Prefab 接入，AgentId 留空使用焦点。方案包含焦点和显式路由对照，不把只给固定 AgentId 下令视作覆盖默认玩家行为。
- Picker 排除 EnemySource，验证仅纳入资源、活跃敌人和撤离群。绕过鼠标意味着不覆盖命中和 UI 遮挡，报告必须保留这个边界。
- R-C1/R-C2 是源码风险，尚无本轮红灯；远敌首次接近和群后备候选作为建议行为供 Review，不能由测试先行悄悄决定玩法。
- 敌人群当前绑定具体成员，未扩大为清群队列。战死和满包规则沿用用户已确认约定。

## 2. 文件和依赖

| 项目 | 自审结论 |
| --- | --- |
| 运行器复用 | 沿用 SceneRaid 生命周期、进程隔离、保存路径、渲染和帧采样，不另建 Player 工程或进程管理器 |
| 新模式 | ManualCluster 显式启用，原 Autonomous/Observe 不自动获得发令能力；普通 Player 编译边界保持 |
| 职责分离 | 脚本 DTO、目标目录、步骤驱动、命令证据、库存只读包装和离线契约各有独立原因，RunController 只编排 |
| 生产入口 | Dispatcher 负责把群转成命令，测试不直接塞入“已选正确成员”的请求，不用人工写状态掩盖缺陷 |
| 库存所有权 | 背包操作继续归现有 Driver；携带探针只读，不调用可能同步库存的结算查询来冒充无副作用读取 |
| 报告复用 | 从旧契约提取共享终态/结算，自主特定覆盖留在原入口，手动特定覆盖放独立模块，不复制两套仓库规则 |
| 错误关联 | 同步早期拒绝通过 attempt 调用边界关联，异步结果用 CommandId；禁止按原因或整帧白名单吞错 |
| 依赖方向 | Automation → Gameplay，Editor → Automation，工具读取文件；没有 Gameplay → Automation/Editor |
| 文件清单 | Reuse/Extend/Wrap/Create 均落到具体文件，测试用例与运行器自检分开；无需新增业务 asmdef |

## 3. 测试与证据

- 明确区分构造布局、真实场景命令和自主尾段，不把构造 Warp/人工伤害伪装成自然整局。
- Accepted 只表示接收，步骤还需执行和终态证据。有限连发结束后检查最新任务有进展，不把无限重试当解锁手段。
- 选择规则、近远定义、触发条件、预算和预期结果先固定；实际场景没有候选时记覆盖不足，不修改阈值或筛掉失败对象。
- 启动器完整归档脚本，显式序列化深度、版本和哈希，Editor/Player 消费同一配置；往返探针检查深层步骤没有被截断。
- 负例预期拒绝仍计入原始事件；未知 Failed、误路由、错误原因、重复结果和丢事件均会失败。先验证报告反例再跑正式矩阵。
- 直接撤离不能假定无库存，需记录预置装备和各角色携带状态；不以最终仓库反推预期。只读快照缺失时报告失败。
- 死亡允许正常终态，不自动补齐未执行步骤；通过固定独立构造弥补覆盖，不提高生命或筛选生还种子。
- 4×用于逻辑，1×用于关键时序和平均 FPS >60，保留慢帧和运行/暂停分项；不扩大为全硬件、全部输入序列无 bug 承诺。

## 4. 实施前要保持的约束

P0 需要完成实际目标身份/路径清单、脚本分局、固定局数和期限，当前不填写虚构的目标坐标或运行次数。P1 若发现规则分歧，先按规划 Review 的行为基准处理；生产公共接口或重要状态职责变化另列小规划说明。每阶段在 `pN_execution.md` 回写实际修改、红绿证据、回归和提交号，本文件追加该阶段架构审查。

当前代码、场景和运行工具未修改。该文档是方案自审，不能代替实施后的代码审查或测试验收。

规划文件已完成 UTF-8、Markdown 围栏和本地引用检查，关键复用路径与仓库文件一致；P0 的 Catalog 调查先于 P2 完整驱动，依赖顺序已明确。

## P0 实施审查

- 用户已确认两项行为基准和自主阶段闭环。Catalog 只依赖已有只读状态/身份和 Unity 路径查询，没有命令接收器调用、路径设置、状态刷新或业务对象创建；两个构造验证角色指令、焦点、导航路径、目标状态保持。
- 新捕获开关默认 false，仅显式观察请求等待两名角色导航就绪后捕获一次，成本单独记录；没有逐帧全群扫描或扩张普通 Player。
- 自检 2/2、真实场景零输入观察通过，保留首次夹具计数错误。来源群附带创建的活跃群没有被错误过滤。
- 真实 R=200 已写入清单，Near Extraction 缺口通过正式行走后的重复指令覆盖；不修改距离定义或角色位置。资源 pivot 路径只作调查，后备选择仍须通过正式入口红/绿验证。
- 11 个最终运行槽位及六个脚本已列入 P0 机器清单。当前只完成 P0，不能把零输入观察写成玩家指令已验证。

## P1a 实施审查

- 初次接近/观察后的状态归 EngageEnemyActionNode，随进入和 CommandId 更换重置；生命周期 IsVisible 每帧核对当前命令目标，没有读取其他敌人的发现事实。手动任务限定新规则，自主任务和伤害反击保持有限追踪。
- TargetClusterDirectiveCandidateSelector 只对工厂拥有的非空候选列表排序和调用既有 Validation，不提交指令、不发布结果、不读取 Automation。Extraction 工厂已有同条件非空检查，Enemy 工厂显式处理空群；同步枚举无中途对象变更。
- 全部候选失败仍由正式接收器报告首候选失败，不静默接受、不吞掉错误。跨高差可射击的既有校验和 7 项战斗接近回归保留。
- 10 项构造、23 项相邻回归通过；固定初始路径预算的移动目标反例通过。只新增显式命令时的候选校验和一次预算计算，没有扩大逐帧全场扫描。
- 无公共接口、模块依赖和场景配置变更，符合已确认的 R-C1/R-C2 边界。P1b 和真实场景成绩仍待执行。

## P1b 实施审查

- 指令转换、库存会话分为两个 Editor 测试文件，工厂只增加正式箱群包装和激活前的敌人生命参数，默认行为保持。生产模块没有测试依赖或状态改写。
- 迟到 Finish 只对被替换的旧身份注入，最新命令靠真实移动/射击/会话关闭推进，没有手动清新任务。双人同箱核对两个独立角色库存总和，背包用例保留 1×/4×时钟恢复。
- 首轮 37/38，失败为测试对已销毁敌人的访问；修正后只复跑受影响单例。覆盖清单由两份 NUnit XML 合并核对，不隐藏首次失败。
- 此阶段检查的是转换和执行进展，未将它升级为整局结算或 FPS 通过。直接早撤库存风险转入下一小阶段红灯构造。

## P1c 实施审查

- 真正存在的非焦点先撤离故障由库存初始状态缺失引起，修复在原库存所有者内完成；没有让 RaidFlow 把缺失快照当空，也没有让测试先切焦点制造库存。
- 当前 UI 初始物品先按原流程保存，其他角色得到独立默认包快照，晚注册使用同一入口；初始化不访问外部箱子或修改会话。单独的已初始化身份集合用于区分首次创建和异常丢失，故障注入证明不会重复创建空库存掩盖丢失。
- 注册列表按索引遍历，已存在角色没有物品复制或逐帧分配；没有新增公共事件/API、场景依赖或通用注册框架。
- 32 项相关构造合并通过，44 项契约反例通过；包含两种真实到点早撤顺序、后注册期间开包不被打断、已有快照缺失继续报错。唯一相邻旧断言漂移已按历史提交核对后更新，错误、物品保留和失败终态检查未削弱。

## P2a 实施审查

- DTO 只校验固定字段值、前序引用和期限，不执行任意代码。Evidence 只调用正式 Dispatcher，目标由外部编排提供，不包含选择策略。
- Observer 保留原始 directive 事件，转发其序号；同步结果在一次调用边界内唯一匹配，之后依 CommandId 关联，旧任务取消不会误配给新 attempt。诊断异常由 Observer 捕获标记，正式命令不受探针异常传播干扰。
- 携带包装只读实际 UI/私有快照，结果转换为不包含业务对象引用的扁平 DTO；没有调用同步结算查询、初始化缺失库存、切焦点或关闭会话。字段类型漂移明确失败。
- 13 项自检合并通过，保留两次共享文件访问的测试错误和修正证据。测试/Automation → Gameplay 的方向不变，普通 Player 仍由条件编译排除。

## P2b/P2c 实施审查（真实冒烟进行中）

- 步骤驱动只持有有界配置、等待起点和已选择群引用；Catalog 的全量路径调查只在下令时执行。RunController 负责动作帧归属和资源生命周期，库存策略没有复制进命令文件。
- 变化触发的进展探针归命令证据，记录实际位置、角色血量、当前/挂起命令、敌人血量和资源身份；最终解释由 PowerShell 完成，不由驱动自报通过。终态回调捕获同帧死亡，构造已验证 50 → 0。
- 共享仓库/布局/会话守恒提取到 Settlement，自主覆盖保留原入口。新 Trace 用原始 sequence 精确核对拒绝，原始失败数单独保留；候选规则在离线端用坐标独立复算，不信任选中标签。
- 18 项 Harness + 11 项库存构造通过，旧 44 项结算、78 项报告通过。53 项新契约探针包含未开箱直接撤离预置物品守恒，取消反例按正式撤离销毁语义修正后 `045441-940` 再次通过。
- 常驻 Editor 的新模式迁移发现完整校验早于源码刷新，已在原 EditorEntry 内调整读取顺序，实际执行仍保留完整隔离校验。兼容 SC00 `045446-468` 成功刷新并保留 PID 11628，不以重启 Editor 绕过生命周期问题。
- 当前未新增生产公共接口或更改场景；P2 真实闭环、P3/P4 矩阵尚未验收。

P2 收尾：MC01-X `045558-205` 双人撤离、3 条命令真实执行通过，Actor2 Near 重复步骤因提前撤离而缺覆盖；SC02 `045721-786` 10 个真实库存会话、完整自主双人搜打撤通过。两轮均无错误/失败指令/停滞，原库存与覆盖契约保留。P2 真实闭环已完成，P3/P4 仍待执行。

## P3a/P3b 审查

- Enum 兼容别名仍保留在生产 Data，稳定语义编码在 Automation Evidence 边界解决；真实击杀用例核对 Engage 字符串，场景复跑类型正确。
- 导航诊断仅在调用者已有 Buffer 内保存内部固定字符串，不改公共结果、路径控制或重试策略。Motor 只在实际执行失败时打印原分支，普通 Player 由 Conditional 移除调用，候选查询不打印。
- 原实验室失败坐标形成单独真实场景构造并实际走完，通用断开岛断言归 NavigationExecutionTests。新增实际路径和 9 项导航执行回归通过，没有把一次未复现误称生产缺陷已修复。
- MC01-R/E 和 MC02 已运行，死亡、缺前提与错误分别记录。MC01-E 首轮的 LostSight 和一次 Unreachable 原始失败均保留，不全局忽略该类结果。MC03/MC04-N 和最终矩阵继续。

## P3c 审查

- 已销毁目标的身份归命令接受时既有 attempt，Evidence 在对象不可读时复用该字符串，不创建额外缓存、修改游戏生命周期或放宽日志一致性断言。
- 新构造明确模拟“对象销毁早于完成回调”，与真实击杀用例分别断言；两项通过。损坏反馈改为 evidence invalid 分类，原 53 项离线反例仍通过。
- MC03 `051818-780` 八项全部触发、两人撤离，证据、指令、背包会话和仓库契约通过。失败及复跑按原脚本分别归档，没有更换目标规则。

P3 六脚本已执行，MC04-N 因交战被反击替换而缺后续负例前提，仍保留 PARTIAL；没有让测试篡改存活或完成状态。剩余一次导航失败的失败时目标尚不能从旧回调重建，保留未确认根因和新的准确分支探针，进入最终固定矩阵观察，不加入未经证明的重试/延时修复。

## P3d / P3e 实施审查

- 真实失败分支已收窄到完整查询后 SetPath 返回 false，不能由此推断空间断开。有限交付预算属于 Navigation 独立状态，Motor 保持查询和实际移动所有权；成功、新命令、显式 Stop 清理，重算不能延期。原空间失败和进展超时仍保留，Gameplay 没有测试注入或 Automation 引用。
- 两项预算边界、九项原导航执行、三项实验室实际行走构造通过。构造没有复现原生拒绝，明确保留此边界；不把日志降级当成恢复证据。最终真实矩阵继续收集 SetPathRetry 后的同命令进展。
- LostSight 仍按已确认的观察后有限追踪规则执行；Evidence 只增加可见性变化采样和原始期限，离线契约逐序号证明预期终止。没有修改生命、追踪规则或全局忽略 Failed。新增 8 项反例、原 53 项命令反例、78 项报告回归和一项实际击杀探针通过。
- `054002-520` 在真实场景验证分类、释放、正常死亡和幸存者结算。首次最终矩阵两局保留为失效版本记录，完成本次提交后重新运行原 11 槽位；不改变脚本、种子和期限，也不通过筛选生还结果达标。

## P3f 实施审查

- 资源完成聚合继续归 ResourceClusterAuthoring；移动节点在查询之后使用现有 Validation 判断完成，不复制成员扫描，不让 Lifecycle 每帧主动刷新全部群。原空间无效分支未放宽。
- 查询刷新可能晚于本帧 Lifecycle 的事实由确定性夹具直接验证。完成走原 Finish 和 CommandId 保护，返回 Running 避免本帧 Search 子节点消费清空的指令，下一正式任务能实际移动。
- 新测试文件仅拥有资源刷新/执行时序构造，与 UI 会话测试分离；没有生产到测试依赖，也没有新的公共接口。1×/4×耗尽两项原红灯，修复后的 6 项边界和原 22 项库存/导航回归全部通过。
- 原 1731 实局 `061033-040` 零失败、零错误，正常死亡/幸存者结算通过。没有将死亡视作 bug，未改变测试种子或目标。第二版九局完整归档，P4 重新冻结原 11 槽位。

## P3g 实施审查

- 无导航锚点的高处敌人不再依赖一条不存在的 partial path；候选生成、路径和射击位置检查仍归 CombatApproachQuery，同文件私有方法复用路径角点处理。使用 agentTypeID/areaMask 采样，完整路径和真实缩放/baseOffset、转向后枪口约束保留。没有宽松接受部分路径或修改敌人位置。
- 自动受击来源在原 Pawn 转命令入口筛选，已有有效反击直接保持，避免重复身份和查询。不可行来源从未成为正式任务，没有把已发出的 Failed 从报告抹掉。Lifecycle 只保留 NavigationNotReady 的等待豁免，直接调用者仍收到其他正式拒绝。
- 新测试文件集中于高差接近/反击，12 项含 1×/4×、实际子弹、墙体/枪口/高度阴性、无效来源仍扣血且旧撤离移动、重新可行后反击、导航恢复和真实场景记录点。合并旧相关组 54 个 Passed 名称，未引入新的公共接口和 Gameplay → Automation 依赖。
- 现场构造在真实地形上走到射击位置并令 Sentinel 血量变化；原 MC02 完整 7/7、双人撤离、零失败。地面候选最多 17 个，已有直接/partial 路径优先；实际 1× 平均帧率继续按最终矩阵测量，不以构造或 4× 帧率代替。
- 第三版矩阵四局保留为历史失败记录，最终再次冻结全矩阵，死亡仍不驱动修复。

## P3h 实施审查

- 原生目的地请求单独归 `AgentDestinationRecovery.cs`，只拥有请求目的地和完成判定；Motor 继续拥有完整空间校验、查询缓存、唯一命令、移动取消和原进展计时，固定交付期限仍由 `AgentPathAssignmentBudget.cs` 管理。依赖单向，没有生产到测试依赖、场景修改或命令公共接口变化。
- 原 SetPath 成功分支不变，仅真实拒绝后进入恢复。异步接受并不算成功，必须结束 pending、完整且未过期、原生路径终点与已采样目的地相符；部分路径、错误终点和失效请求不能通过。挂起期间不连续覆盖异步请求，暂停不消耗游戏时间，改令/Stop 清状态并取消旧原生路径。
- 确定性红灯证明普通重查、清旧路径、换路径对象、短拐点或终点微调均不能解决；同一状态下原生目的地请求产生完整路径并实际移动，随后恢复普通交付。临时诊断干预已从最终构造中删除，未把内部 C++ 原因未经证实的推测写成结论。
- `070843-109` 与 `071259-423` 合并 65 个不同 Passed 名称，完整登记匹配为 Navigation 17、SceneResourceNavigation 26、ClusterCommandReachability 10、ElevatedCombatApproach 12。包括原记录路径 15 种起点/帧步长、真实拒绝后暂停/替换、完整/部分/错误终点和旧空间阴性。保留第一次 61/65 及新夹具坐标修正、旧 1× 限时击中用例单次超时记录；复跑没有扩大期限或修改伤害。
- 正常路径仅增加每个 Motor 的小恢复对象和分支判断，pending 轮询为 O(1)，只在拒绝后请求异步路径。路径角点数沿用已有数组查询，仅失败/恢复事件生成诊断字符串；1× FPS 继续由最终实局验证，不以固定 captureDeltaTime 构造成绩代替。

## P3i 实施审查

- 多角色物理 presence 归既有 ExtractionPointController，RaidFlow 的独立读条和正式库存结算保持。每角色聚合 Collider，进出只更新所属角色；禁用组件清理并拒绝迟到物理消息，失效 Collider 不再遗留计时。沿用原水平范围和状态变化通知，避免每帧重复进入抵消动作层的中断。
- Update 仅遍历当前重叠 Collider，清理列表复用，不逐帧扫描全场 Agent；新私有数据没有扩张模块公共接口。没有改身体、触发器尺寸、导航或生命，普通 Player 不依赖 Automation。
- 只读撤离探针复用 SceneRaidReadModel 的私有字段适配和缓存，记录真实每角色点位与进度；缺失字段仍失败，不创建或重置计时。新增测试独立覆盖物理入口，旧终态测试继续拥有流程层边界，没有重复搬迁结算逻辑。
- 14 项新物理构造、80 项相关回归全部通过，并核对完整 NUnit 名称。保留首次 9 项红灯和夹具注册错误，实际 Prefab 下两种倍速均可双人正式撤离。最终整局和 FPS 仍待 P3j 后的新固定矩阵。

## P3j 实施审查

- 帧容量由既有 SceneRaidFrameSampler 根据有限墙钟预算拥有，RunController 只传入已配置期限，RunResult 只增加容量事实。没有新 Gameplay 依赖、线程、帧率控制或对报告规则的放宽。
- 仍按实际样本增长，超限不继续扩容且原 HARNESS_FAILED 保留；无效期限在订阅渲染回调前拒绝。每次采样新增判断为 O(1)，录制内容、时钟和 CSV 格式不变，实际内存边界随预算增加并在规划中说明。
- 超旧容量红灯及 4 项采样构造完成，79 项报告回归通过，新增精确失败终态反例。构造不证明实际 FPS；第五版历史截断仍失败，重新冻结最终矩阵验证真实渲染和完整帧证据。

## P4b 成本补测审查

- 新 ClusterCommandCostTests 独立拥有同步提交测量与夹具，复用原世界/目标工厂和正式 Dispatcher；没有修改 Gameplay、Automation 或公共接口，没有将成本逻辑堆进转换、库存测试。记录格式化在采样之后，两个 Recorder 都在调用结束后停止并释放。
- 当前线程逐样本测量经空调用、一次/两次已知分配和两次实际导航校准；失败的 Mono 字节 API 与纳秒 GC 标记值均不进入最终字节结论。13/13 和 120 条记录完整，首次资源计数为零，同帧边界、容量和最后真实移动均检查。
- 计数只解释对应入口，资源实际计算与一般导航检查分列，不伪造单次字节数或把构造当作真实场景 FPS。初次调用不声称冷进程，固定有限重复保留后续分配事实。
- 与最终场景基线的哈希差异严格为新增测试、meta、登记清单三项，实际游戏和运行器未变。无需重跑不受影响的图形矩阵，两个数据集分别保留源码与测量环境。
